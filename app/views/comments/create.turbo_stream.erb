<%= turbo_stream.replace "post#{@post.id}comments", partial: 'posts/post_comments', locals: { post: @post } %>
<%= turbo_stream.replace "post#{@post.id}Modalcomments", partial: 'posts/post_comments', locals: { post: @post } %>
