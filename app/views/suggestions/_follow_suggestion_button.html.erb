<%= button_to follow_path(user_id: suggestion.id), method: :post, class: "btn bg-transparent border-0", data: { "turbo-action": "replace", "turbo-frame": "follows_list" } do %>
  <small class="fw-bold text-primary">Follow</small>
<% end %>
