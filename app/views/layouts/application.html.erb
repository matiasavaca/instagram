<!DOCTYPE html>
<html>
  <head>
    <title>Instagram</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://unpkg.com/filepond@4.x/dist/filepond.css" rel="stylesheet">
    <script src="https://unpkg.com/filepond@4.x/dist/filepond.js"></script>
  <style>
      <%= ".msg-#{current_user&.id}"%>{
        background-color: #007bff !important;
        padding: 10px;
      }

      <%= ".cont-#{current_user&.id}"%>{
        display: flex;
        justify-content: flex-end;
      }

  </style>

  </head>

  <body class="app-container <%= controller_name %>"
      style="<%= 'overflow-y: hidden;' if controller_name == 'rooms' || (controller_name == 'users' && action_name == 'direct_message') %>">
    <% if current_user %>
      <%= render 'shared/navbar' %>
    <% end %>
    <%= yield %>
  </body>
</html>
